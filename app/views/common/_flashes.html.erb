<% if flash.any? %>
   <% user_facing_flashes.each do |key, value| %>
      <div class="alert alert_dismissable alert-<%= flash_class(key) %>">
           <button "close" data-dismiss="alert">x</button>
            <%= value %>
      </div>
      <% end %>
<% end %>
